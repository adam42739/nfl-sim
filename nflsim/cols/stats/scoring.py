from nflsim.cols.stats import types


GP = "GP"
PASS = "PASS"
RUSH = "RUSH"
REC = "REC"
RET = "RET"
TD = "TD"
TWOPT = "2PT"
PAT = "PAT"
FG = "FG"
PTS = "PTS"

DICT = {
    GP: types.ACCUM,
    PASS: types.ACCUM,
    RUSH: types.ACCUM,
    REC: types.ACCUM,
    RET: types.ACCUM,
    TD: types.ACCUM,
    TWOPT: types.ACCUM,
    PAT: types.ACCUM,
    FG: types.ACCUM,
    PTS: types.ACCUM,
}
